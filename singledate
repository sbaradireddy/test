The test cell works perfectly — pandas ok, S3 write ok, Spark read ok, cast ok. That means the `make_spark_df` function itself is fine.

The error is coming from somewhere **before** Cell 8 — specifically the `F.col()` cast calls inside `make_spark_df` are fine, but something in Cells 5, 6, or 7 is still calling a Spark function with a float.

The culprit is this line in `analyse_narrow` Cell 6:

```python
from pyspark.sql import functions as _F
cast_list.append(_F.col(c).cast("bigint").alias(c))
```

Wait — Cell 6 has no Spark. Let me look at what's actually different. The test works but the real code fails. The difference is **`value_pct`** — in your real data `46.607421875` is a float being passed somewhere as a column name. This means one of your **column names in the actual parquet file** is a float number like `46.607421875`.

Run this one cell to confirm:

```python
# Run this to find the exact bad column name
import io
import boto3
import pyarrow.parquet as pq

for st in schema_types:
    entries = by_schema[st]
    entry   = entries[0]
    body    = boto3.resource("s3").Object(
        SOURCE_BUCKET, entry["s3_key"]).get()["Body"].read()
    buf     = io.BytesIO(body)
    tbl     = pq.read_table(buf)
    for i in range(len(tbl.schema)):
        field     = tbl.schema.field(i)
        name      = field.name
        name_type = type(name).__name__
        if name_type != "str":
            print("FOUND IT: schema=" + st
                  + "  col_index=" + str(i)
                  + "  name=" + str(name)
                  + "  type=" + name_type)
    # Also check if any column name looks like a float
    for i in range(len(tbl.schema)):
        name = tbl.schema.field(i).name
        try:
            float(str(name))
            print("FLOAT NAME: schema=" + st
                  + "  col=" + str(name))
        except ValueError:
            pass
    del tbl, buf, body

print("scan complete")
```
