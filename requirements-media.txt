# Add to your existing requirements.txt to enable audio/image/video parsing
boto3>=1.34.0             # Rekognition (image_parser.py), Bedrock Nova 2 Lite (audio_parser.py)
Pillow>=10.0.0            # image metadata (media_metadata.py), face blurring (pii_processor.py)
faster-whisper>=1.0.0     # OPTIONAL - only needed if redact_audio_pii=True. AudioParser
                           # transcribes via Bedrock (no local model needed); this is used
                           # solely by AudioPiiProcessor in pii_processor.py, which needs
                           # local per-segment timestamps to mute audio ranges - something
                           # Bedrock's Converse API doesn't return. Skip this dependency
                           # entirely if you don't enable redact_audio_pii.

# System-level (not pip-installable) - must be present on the container/image:
#   ffmpeg / ffprobe  -> apt-get install -y ffmpeg
